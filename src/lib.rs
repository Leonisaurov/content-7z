pub mod files {
    pub mod entry;
    pub mod folder;
}

pub mod window {
    pub mod cursor;
    pub mod window;
    pub mod scheme;
    pub mod handler;
}

pub mod zip_manager {
    pub mod manager;
}

pub mod config;
